services:
  cg-control:
    build: .
    container_name: cg-control-pro
    ports:
      - "3000:3000"
    volumes:
      - ./database.sqlite:/app/packages/server/database.sqlite:Z
    environment:
      - PORT=3000
      - DATABASE_URL=/app/packages/server/database.sqlite
    restart: unless-stopped
