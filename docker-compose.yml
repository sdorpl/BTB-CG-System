services:
  cg-system:
    build: .
    container_name: btb-cg-system
    ports:
      - "3000:3000"
    volumes:
      - cg-data:/data
    environment:
      - PORT=3000
    restart: unless-stopped

volumes:
  cg-data:
