{
  "id": "republika-clock",
  "name": "Republika Clock",
  "type": "CLOCK",
  "version": 1,
  "html_template": "<div class=\"rep-left-panel\">\n    <div class=\"rep-clock\">00:00</div>\n</div>",
  "css_template": "#{{ID}} .rep-left-panel {\n    z-index: 50;\n    position: absolute;\n    top: 0;\n    left: 0;\n    width: 150px;\n    height: 40px;\n    background: {{PRIMARY_BG}};\n    color: {{TITLE_COLOR}};\n    display: flex;\n    flex-direction: column;\n    overflow: hidden;\n    transform-origin: left center;\n    font-family: '{{FONT_FAMILY}}', 'Roboto Condensed', sans-serif;\n    box-shadow: {{BOX_SHADOW}};\n    opacity: 0;\n    transform: {{ANIMATION_IN_TRANSFORM}};\n    transition: all {{ANIMATION_DURATION}}s {{ANIMATION_EASE}};\n    border-right: 2px solid rgba(255,255,255,0.1);\n}\n#{{ID}} .rep-clock {\n    height: 40px;\n    background: {{PRIMARY_BG}};\n    display: flex;\n    align-items: center;\n    justify-content: center;\n    font-size: 28px;\n    font-weight: 700;\n    letter-spacing: 1px;\n}",
  "js_template": "(() => {\n    const root = document.getElementById(\"{{ID}}\");\n    const container = root.querySelector(\".rep-left-panel\");\n    const clockEl = root.querySelector(\".rep-clock\");\n    let timer;\n    const updateTime = () => {\n        const now = new Date();\n        const h = String(now.getHours());\n        const m = String(now.getMinutes()).padStart(2, \"0\");\n        if(clockEl) clockEl.textContent = h + \":\" + m;\n    };\n    root.__slt_show = () => {\n        const tl = GSAPFX.standardIn(container, {{{ANIMATION_IN_JSON}}});\n        const textTl = GSAPFX.applyTextEffect(root, {{{TEXT_ANIM_JSON}}});\n        if (textTl) {\n            if ({{{TEXT_ANIM_SYNC}}}) {\n                tl.add(textTl, \">-0.1\");\n            } else {\n                tl.add(textTl, 0);\n            }\n        }\n        updateTime();\n        timer = setInterval(updateTime, 1000);\n        return tl;\n    };\n    root.__slt_hide = () => {\n        if (typeof timer !== \"undefined\") clearInterval(timer);\n        const textTl = GSAPFX.applyTextOutEffect(root, {{{TEXT_ANIM_OUT_JSON}}});\n        const tl = GSAPFX.standardOut(container, {{{ANIMATION_OUT_JSON}}});\n        if (textTl) {\n            const sync = {{{TEXT_ANIM_OUT_SYNC}}};\n            if (sync) {\n                const combined = gsap.timeline();\n                combined.add(textTl);\n                combined.add(tl, \">-0.1\");\n                return combined;\n            } else {\n                tl.add(textTl, 0);\n            }\n        }\n        return tl;\n    };\n})();",
  "defaultFields": {},
  "defaultLayout": {
    "x": 96,
    "y": 910
  },
  "ocgInputs": []
}